@model IEnumerable<GuidanceTracker.Models.DetailedFeedback>
@{
    ViewBag.Title = "Detailed Feedback";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col">
            <h2><i class="fas fa-clipboard-list"></i> Detailed Feedback</h2>
            <p class="text-muted">Comprehensive feedback received from students</p>
        </div>
        <div class="col-auto">
            <a href="javascript:history.back()" class="btn" style="background-color: #f2e9ff; color: #7F3D98; border-color: #7F3D98; ">
                <i class="fas fa-arrow-left"></i> Back
            </a>
            <a href="@Url.Action("CreateDetailedFeedbackRequest", "DetailedFeedback")" class="btn" style="background-color: #7F3D98; color: white;">
                <i class="fas fa-plus"></i> Request Feedback
            </a>
        </div>
    </div>

    @if (!Model.Any())
    {
        <div class="alert" style="background-color: #e0c6ff; color: #4b0082; border-color: #7F3D98;">
            <i class="fas fa-info-circle"></i> You haven't received any detailed feedback yet.
            <a href="@Url.Action("CreateDetailedFeedbackRequest", "DetailedFeedback")" class="alert-link" style="color: #7F3D98;">
                Request detailed feedback from your students
            </a>
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-header" style="background-color: #7F3D98; color: white;">
                <h4>Received Feedback</h4>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead style="background-color: #f2e9ff;">
                            <tr>
                                <th>Student</th>
                                <th>Class</th>
                                <th>Course</th>
                                <th>Date</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.OrderByDescending(f => f.DateCreated))
                            {
                                <tr>
                                    <td>
                                        @if (item.Student != null)
                                        {
                                            <span>@item.Student.FirstName @item.Student.LastName</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Unknown</span>
                                        }
                                    </td>
                                    <td>@item.Class</td>
                                    <td>@item.Course</td>
                                    <td>
                                        @(item.DateCreated.ToString("MMM d, yyyy"))
                                    </td>
                                    <td>
                                        <a href="@Url.Action("ViewDetailedFeedbackDetails", "DetailedFeedback", new { id = item.FeedbackId })"
                                           class="btn btn-sm" style="background-color: #7F3D98; color: white;">
                                            <i class="fas fa-eye"></i> View
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
</div>